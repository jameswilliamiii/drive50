<div id="recent-drives-table" class="drive-list">
  <% if recent_sessions.any? %>
    <%= render partial: "drive_sessions/drive_row", collection: recent_sessions, as: :session %>
  <% else %>
    <p class="empty-state">No drives recorded yet. Start your first drive!</p>
  <% end %>
</div>
