# CLAUDE.md

This project keeps a single source of truth for AI coding agents in
[`AGENTS.md`](AGENTS.md), shared across tools (Claude Code, Codex, Cursor,
OpenCode). Do not add project guidance here — edit `AGENTS.md` instead.

Claude Code imports it automatically:

@AGENTS.md
